# Python dependencies for AEPGP Windows Context Menu Integration

# Smart card communication library
pyscard>=2.0.0

# OpenPGP encryption/decryption without GPG
PGPy>=0.6.0

# For parsing certificates and keys
cryptography>=41.0.0

# For building executable (development only - not needed for MSI installation)
# pyinstaller>=5.0.0

# ---------------------------------------------------------------------------
# EXTERNAL RUNTIME DEPENDENCY (not pip-installable)
# ---------------------------------------------------------------------------
# GPG (gpg.exe) is required by change_pin_handler.py to change the card PIN.
# It is NOT a Python package and cannot be installed via pip.
#
# Install GPG4Win from: https://www.gpg4win.org/
# Ensure that gpg.exe is on the system PATH after installation.
# INSTALL.bat will warn you if gpg.exe is not found.
# ---------------------------------------------------------------------------
